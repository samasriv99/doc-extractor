clone the repository
make the repository as the current working directory
create a virtual environment
install the requirements
set your GEMINI_API_KEY in your environment
go one level up the current directory
run python3 docextract.api.main and set url to http:/localhost:8000/
